#!/data/data/com.termux/files/usr/bin/sh
# Clear the killswitch so automation can run again.
rm -f "$HOME/.local_agent/STOP"
command -v termux-notification >/dev/null 2>&1 && \
  termux-notification --title "local-agent" --content "🟢 killswitch cleared"
echo "killswitch cleared"
